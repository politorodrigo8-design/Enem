import { AiLoadingSkeleton } from "../ai-loading-skeleton";

export function QuestionExplanationSkeleton() {
  return <AiLoadingSkeleton title="Preparando uma explicação personalizada..." rows={5} />;
}
