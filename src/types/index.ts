export interface EvolutionPoint {
  label: string;
  score: number;
  accuracy: number;
}

export interface CreditPackage {
  id: string;
  title: string;
  credits: number;
  price: string;
  description: string;
}
